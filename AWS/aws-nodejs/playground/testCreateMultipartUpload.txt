var AWS = require('aws-sdk');

// Create an S3 client
var s3 = new AWS.S3();

var params = {
    Bucket: 'rtc-platform-fusion-test-artifacts',
    Key: 'testCompleteMultipartUpload.txt', /* required */
};
s3

s3.createMultipartUpload(params, function(err, data) {
  if (err){
      console.log(`==>error:`);
      console.log(err)
  } else {
      console.log(`==>data: `);
      console.log(data);
  }
});
